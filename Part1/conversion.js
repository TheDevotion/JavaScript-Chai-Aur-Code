let id = "123";

let NewId = Number(id);
console.log(NewId);
console.log(typeof NewId);

/* Same way you can convert number to string by using String.
remember that, First letter must be capital.
Null, Undefined, Object etc. */
